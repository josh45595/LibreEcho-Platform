# LibreEcho embedded/source upstream audit inventory.
#
# Renovate treats entries in this file as review signals, not as permission to
# change a release input automatically. Updates are held in the Dependency
# Dashboard until explicitly approved. Any approved update must still refresh
# the corresponding source archive/commit, hashes, notices/SBOM data and pass
# the normal ARM32 build/runtime verification before merge.

# AirPlay 2 receiver. Current Platform documentation/build input: 5.1.
# renovate: datasource=github-releases depName=mikebrady/shairport-sync versioning=loose
SHAIRPORT_SYNC_VERSION=5.1

# AirPlay 2 timing daemon. Current Platform documentation/build input: 1.2.8.
# renovate: datasource=github-releases depName=mikebrady/nqptp versioning=loose
NQPTP_VERSION=1.2.8
